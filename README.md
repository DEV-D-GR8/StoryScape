# StoryScape
A storytelling (generates stories with pictures) generative AI based iOS application based on custom fine tuned LLaMA 3.1 8B-Instruct model on Hindi stories (Provision to generate English stories via call to OpenAI GPT-4o).

# Code and details will be uploaded soon.
